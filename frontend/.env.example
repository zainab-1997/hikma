# Public API origin only; Vite variables are visible in the browser and must never contain secrets.
# Set this explicitly to the HTTPS API origin for production builds.
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_APP_VERSION=1.0.0-rc1
